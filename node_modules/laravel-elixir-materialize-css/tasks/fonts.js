var Elixir = require('laravel-elixir');
var Config = require('../Config');

/**
 * Copy over the material images.
 */
Elixir.extend('materializeFonts', function() {
    Elixir(function(mix) {
        mix.copy(Config.matPath + 'fonts', Config.fontsOutputFolder);
    });
});

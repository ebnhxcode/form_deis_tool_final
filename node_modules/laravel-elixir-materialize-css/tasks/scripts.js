var Elixir = require('laravel-elixir');
var Config = require('../Config');

/**
 * Build the material JavaScripts.
 */
Elixir.extend('materializeJs', function() {
    Elixir(function(mix) {
        mix.scripts([
            // './' + Config.matPath + 'js/date_piker/picker.js',
            // './' + Config.matPath + 'js/date_piker/picker.date.js',
            // './' + Config.matPath + 'js/*.js',
            './' + Config.matPath + 'bin/materialize.js'
        ], Config.jsOutputFile)
    });
});

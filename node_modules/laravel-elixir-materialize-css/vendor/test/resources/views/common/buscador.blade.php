<div class="container buscador">
	{!! Form::open(['url' => ['search'], 'method' => 'post', 'id'=>'buscador']) !!}
	<div class="row">
		<div class="col s6 m6">{!! Form::text('buscar', null, ['class' => 'form-control', 'id'=>'buscar', 'placeholder'=>'Buscar']) !!}</div>
		<div class="col s6 m3">{!! Form::text('ubicacion', null, ['class' => 'form-control', 'id'=>'ubicacion', 'placeholder'=>'Ubicación']) !!}</div>
		<div class="col s6 m3">
      {!! App::make('App\Http\Controllers\CategoryController')->select(0) !!}
    </div>
    <div class="col s6 m3">{!! Form::submit('Buscar', ['class' => 'btn btn-primary btn-block']) !!}</div>
    <!--
		<div class="col-md-3 col-sm-6">{!! Form::select('subcategoria', [0=>'Subcategorías'], null, ['class' => 'selectpicker', 'id'=>'subcategoria']) !!}</div>
    -->
	</div>
	
	
  {!! Form::close() !!}
    <div class="options">
    	<ul>
    		<li><a href="{{route('anuncios.index')}}"><i class="mdi mdi-view-list"></i> listado</a></li>
    		<li><a href="{{route('anuncios.mapa')}}"><i class="mdi mdi-map"></i> mapa</a></li>
    	</ul>
    </div>
    <div class="history" id="filters">
   @if( Session::has('filters')) 
      @foreach( Session::get('filters') as $filter=>$values)
        @foreach($values as $value)
        <span class="filter {{ $filter }}" data-type="{{$filter}}">{{$value}}</span>
        @endforeach
      @endforeach
   @endif
  </div>
</div>

@section('css')
@parent
<style>
  .ui-autocomplete-loading {
    background: white url("../img/ui-anim_basic_16x16.gif") right center no-repeat;
  }
  </style>
@endsection

@section('js')
@parent

@endsection
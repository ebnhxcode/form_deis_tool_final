<ul id="slide-out" class="side-nav">
    @if (Auth::guest())
    <li><a class="waves-effect" href="{{ url('/login') }}">Login</a></li>
    <li><a class="waves-effect" href="{{ url('/register') }}">Register</a></li>
    <li><a class="waves-effect" href="{{ url('categorias') }}">Categorías</a></li>
    @else
    <li>
        <div class="userView">
            <img class="background" src="{{ asset('img/office.jpg')}}">
            <a href="{{ url('user/'.Auth::user()->name.'/anuncios/') }}">
                @if(Auth::user()->avatar!="")
                    <img class="circle" src="{{Auth::user()->avatar}}">
                @else 
                    <img class="circle" src="https://www.gravatar.com/avatar/{{ md5(Auth::user()->email) }}">
                @endif
            </a>
            <a href="{{ url('user/'.Auth::user()->name.'/anuncios/') }}"><span class="white-text name">{{ Auth::user()->name }}</span></a>
            <a href="{{ route('user.profile', ['user'=>Auth::user()->name]) }}"><span class="white-text email">{{ Auth::user()->email }}</span></a>
            <div class="row">
                <div class="col s12">
                    <a href="{{ url('anuncios/create') }}" class="btn-floating waves-effect waves-light red"><i class="mdi mdi-library-plus"></i></a>
                    <a href="{{ route('user.profile', ['user'=>Auth::user()->name]) }}" class="btn-floating waves-effect waves-light blue"><i class="mdi mdi-heart"></i></a>
                </div>
            </div>
        </div>
    </li>
    @if(Auth::user() and Auth::user()->isRole('admin'))
    <li><a href="{{ url('admin') }}">Admin</a></li>
    @endif
    <li><a class="waves-effect" href="{{ url('categorias') }}">Categorías</a></li>
    <li><a href="{{ url('/logout') }}">Logout</a></li>
    @endif
    <li>
        <div class="divider"></div>
    </li>
    <li><a class="subheader">Info</a></li>
    <li><a class="waves-effect" href="#!">Faq</a></li>
    <li><a class="waves-effect" href="#!">Sobre nosotros</a></li>
</ul>